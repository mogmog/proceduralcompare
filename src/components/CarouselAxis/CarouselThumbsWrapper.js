import React from 'react';

const CarouselThumbsWrapper = (props) => {
    return (
        <div className="carousel-thumbs-wrapper">
            {props.children}
        </div>
    );
} ;

export default CarouselThumbsWrapper;
